import React from 'react'
import { PaymentListTemplate } from '@poc/templates'
import { ThemeBase } from '@poc/theme'

export const PaymentList = () => {
  return (
    <PaymentListTemplate theme={ThemeBase.Midway} />
  )
}
